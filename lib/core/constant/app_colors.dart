import 'package:flutter/material.dart';

class AppColors {
  // Primary Color
  static const Color primary = Color(0xff2596be);
  static const Color secondary = Color(0xFFefeafb);
  static const Color taritory = Color(0xFFFFFFFF);

  // Background Colors
  static const Color light = Color(0xfff1f1f1);
  static const Color transparent = Colors.transparent;

  // UI Colors
  static const Color border = Colors.grey;

  // Text Colors
  static const Color text = Color(0xFF1D1D1D);
  static const Color textSecondary = Color(0xFFc6c6c8);
  static const Color textDisabled = Color(0xff5b5c61);

}
